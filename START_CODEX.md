# Start the SF3 recompilation experiment

Open Codex in `I:\Projects\SF3-Recomp-Lab` and instruct it to pursue the active
objective in `docs/sf3/CURRENT_OBJECTIVE.md` after reading `AGENTS.md` and every
required source. Runs must remain headless and silent. Preserve deterministic
gates, provenance boundaries, the consult-test-return loop, devlog/report
updates and milestone commits; do not modify sibling implementation projects
or push a remote.
